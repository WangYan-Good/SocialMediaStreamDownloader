# Human-maintained direct dependency policy.  Production and CI install only
# the generated, fully hashed requirements.txt lock.
f2==0.0.1.7
PyYAML==6.0.2
requests==2.32.5
gmssl==3.2.2
ffmpeg-python==0.2.0
selenium==4.43.0
PyMySQL==1.1.2
Flask==3.1.3
waitress==3.0.2
python-dotenv==1.1.0
DBUtils==3.1.0
SQLAlchemy==2.0.51
alembic==1.19.0
httpx==0.27.2
qrcode==8.0

# The repository's test runner is part of the one CI lock.  f2 currently also
# depends on these packages, but they are direct project tooling requirements.
pytest==8.3.4
pytest-asyncio==0.25.0
